database:
  use: SQL
  source:
    SQL:
      host: localhost
      port: 3306
      user: root
      password: root
      database: test
      table: chemdah
    MongoDB:
      client: localhost
      database: test
      collection: chemdah

settings:
  default-progress-meta-content: '&3[{name}] &7{current}/{target} &f[{percent}%]'